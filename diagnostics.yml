openapi: 3.0.3

paths:
  /readiness:
    parameters:
      - $ref: './common.yml#/components/parameters/x-request'
    get:
      tags:
        - diagnostics
      summary: Readiness (TODO)
      operationId: readiness
      responses:
        200:
          $ref: "#/components/responses/ReadinessOK"
        400:
          $ref: "#/components/responses/ReadinessBad"

components:
  responses:
    ReadinessOK:
      content:
        application/json:
          schema:
            type: object
            additionalProperties: true
    ReadinessBad:
      content:
        application/json:
          schema:
            type: object
            additionalProperties: true